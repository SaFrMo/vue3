import * as THREE from 'three'

export default function() {
    // This function will run every frame in your game.
    // You have access to the root Vue instance with `this` and to the
    // main Vue3Game instance with `this.game`.
    //
    // For example, to access an object called "player" and move it
    // forward on the Z axis at 5 units per second:
    //
    // const player = this.game.load('player')
    // if (player) {
    //     player.position.set(
    //         player.position.x,
    //         player.position.y,
    //         player.position.z + 5 * this.game.deltaTime
    //     )
    // }
}
